@{
    int row = 1;
}
@model IEnumerable<LookupModel>
<div class="col-md-12">
    <div class="card patients-list">
        <div class="header">
            <h2><strong>قائمة</strong> معلومات اضافية</h2>
            <ul class="header-dropdown">
            </ul>
        </div>
        <div class="body">
            @if (Model.Any())
            {
                <div class="tab-content m-t-10">
                    <div class="tab-pane table-responsive active table-scroll table table-bordered" id="All">
                        <table class="table m-b-0 table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>@Html.DisplayNameFor(a => a.LookupCode)</th>
                                    <th>تفاصيل</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@row</td>
                                        <td>@item.LookupCodeDesc</td>
                                        <td><a href="@Url.Content("~/ExtraFeature/ExtraFeatureDetailsIndex?lookupId=")@item.Id" style="height: 25px;" class="btn btn-primary btn-round"><i class="zmdi zmdi-edit" style="color: white;margin-top: -10px;"></i></a></td>
                                    </tr>
                                    row += 1;
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            }
            else
            {

                <div class="empty-state-container">
                    <div class="empty-state-icon">
                        <i class="zmdi zmdi-search-for-wild"></i> 

                    </div>
                    <div class="empty-state-title">لا توجد بيانات حالياً</div>
                    <div class="empty-state-description">لا توجد بيانات ليتم عرضها يمكنك اضافة بيانات ليتم عرضها هنا </div>
                </div>
            }
        </div>
    </div>
</div>